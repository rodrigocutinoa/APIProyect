services:
  - type: web
    name: APIProyect
    env: python
    region: oregon
    plan: free
    buildCommand: CARGO_HOME=/tmp/.cargo RUSTUP_HOME=/tmp/.rustup pip install -r requirements.txt
    startCommand: uvicorn src.main:app --host 0.0.0.0 --port $PORT
